{
  "production": "${production}",
  "environmentName": "${environmentName}",
  "apiConfig": {
    "apiUrl": "${apiUrl}",
    "apiVersion": "${apiVersion}"
  },
    "apiUrlIdentity": "${apiUrlIdentity}"
}
